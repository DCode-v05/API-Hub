node_modules/
dist/
out/
*.tsbuildinfo
.DS_Store
npm-debug.log*
coverage/
# local scratch / acquisition output
.cn-tmp/
# generated projection surfaces (regenerable via `cn project`)
surfaces/
surfaces-adv/
__pycache__/
# secrets — never commit real tokens (.env.example is the tracked template)
.env
.env.local
# studio local data store (users, run history, presets, session secret)
apps/studio/.data/
apps/studio/.next/
apps/studio/.next-verify/
# studio-managed hosted MCP servers (written + spawned per deployment)
apps/studio/.hosts/